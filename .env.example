# Client-side environment variables recovered from the extracted snapshot.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Notes:
# - The client also expects companion API routes under /api/security/*,
#   /api/rcui/*, and /api/stripe/*.
# - RCUI export/import depends on a server-side encryption key handled by the
#   companion backend, not by the browser client.
